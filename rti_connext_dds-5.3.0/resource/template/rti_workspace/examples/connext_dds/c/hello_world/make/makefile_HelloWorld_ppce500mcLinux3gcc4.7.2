######################################################################
# makefile_HelloWorld_ppce500mcLinux3gcc4.7.2#
# (c) Copyright, Real-Time Innovations, 2012.  All rights reserved.
# No duplications, whole or partial, manual or electronic, may be made
# without express written permission.  Any such copies, or
# revisions thereof, must display this notice unaltered.
# This code contains trade secrets of Real-Time Innovations, Inc.
#
#
# This makefile was automatically generated by rtiddsgen.
#
# To compile, type:
#         gmake -f makefile_HelloWorld_ppce500mcLinux3gcc4.7.2#
# Note: This makefile is only meant to build our example applications and
#       may require alterations to build on your system.
#
# This makefile assumes that your build environment is already correctly
# configured. (For example, the correct version of your compiler and
# linker should be on your PATH.)
######################################################################


SOURCE_DIR = src/

TARGET_ARCH = ppce500mcLinux3gcc4.7.2

ifndef COMPILER
COMPILER = powerpc-fsl_networking-linux-gcc
endif
COMPILER_FLAGS = -m32 -mhard-float -mcpu=e500mc -Wall -Wno-unknown-pragmas --sysroot=$(SYSROOT_e500mc)/ppce500mc-fsl_networking-linux
ifndef LINKER
LINKER = powerpc-fsl_networking-linux-gcc
endif
LINKER_FLAGS = -m32 -mcpu=e500mc -static-libgcc --sysroot=$(SYSROOT_e500mc)/ppce500mc-fsl_networking-linux
SYSLIBS = -ldl -lnsl -lm -lpthread -lrt
DEFINES = -DRTI_UNIX -DRTI_LINUX -DPOWERPC 
ifndef DEBUG
DEBUG=0 
endif
ifeq ($(DEBUG),1)
COMPILER_FLAGS += -g -O0
LINKER_FLAGS += -g
DEBUG_SFX = d
else
DEBUG_SFX = 
endif
 
ifndef SHAREDLIB
SHAREDLIB=0
endif

ifeq ($(SHAREDLIB),1)
SHAREDLIB_SFX = 
else
SHAREDLIB_SFX = z
DEFINES += -DRTI_STATIC
endif



INCLUDES = -I. -I$(NDDSHOME)/include -I$(NDDSHOME)/include/ndds 
       
LIBS = -L$(NDDSHOME)/lib/$(TARGET_ARCH) \
        \
        \
        -lnddsc$(SHAREDLIB_SFX)$(DEBUG_SFX) -lnddscore$(SHAREDLIB_SFX)$(DEBUG_SFX) $(SYSLIBS) \
       
CDRSOURCES    = HelloWorld.idl
SOURCES = $(SOURCE_DIR)HelloWorldPlugin.c $(SOURCE_DIR)HelloWorld.c $(SOURCE_DIR)HelloWorldSupport.c
COMMONSOURCES = $(notdir $(SOURCES))

EXEC          = HelloWorld_subscriber HelloWorld_publisher
DIRECTORIES   = objs.dir objs/$(TARGET_ARCH).dir
COMMONOBJS    = $(COMMONSOURCES:%.c=objs/$(TARGET_ARCH)/%.o)

# We actually stick the objects in a sub directory to keep your directory clean.
$(TARGET_ARCH) : $(DIRECTORIES) $(COMMONOBJS) \
	$(EXEC:%=objs/$(TARGET_ARCH)/%.o) \
	$(EXEC:%=objs/$(TARGET_ARCH)/%)

objs/$(TARGET_ARCH)/% : objs/$(TARGET_ARCH)/%.o
	$(LINKER) $(LINKER_FLAGS)   -o $@ $@.o $(COMMONOBJS) $(LIBS)

objs/$(TARGET_ARCH)/%.o : $(SOURCE_DIR)%.c   $(SOURCE_DIR)HelloWorld.h 
	$(COMPILER) $(COMPILER_FLAGS)  -o $@ $(DEFINES) $(INCLUDES) -c $<

#
# Uncomment these lines if you want the support files regenerated when idl
# file is modified
#
#
#  HelloWorldPlugin.c HelloWorld.c HelloWorldSupport.c \
#  HelloWorldPlugin.h HelloWorld.h HelloWorldSupport.h : \
#		HelloWorld.idl
#	$(NDDSHOME)/bin/rtiddsgen HelloWorld.idl -replace -language C
#
# Here is how we create those subdirectories automatically.
%.dir : 
	@echo "Checking directory $*"
	@if [ ! -d $* ]; then \
		echo "Making directory $*"; \
		mkdir -p $* ; \
	fi;

